streamlit>=1.20.0
pandas>=1.5.0
numpy>=1.24.0
scikit-learn>=1.2.0
plotly>=5.15.0
prophet>=1.1.0
mlflow>=2.0.0
lightgbm>=3.3.0
statsmodels>=0.13.0
joblib
pyyaml
requests
# Optional / tooling
# dvc  # DVC is not required at runtime in Streamlit Cloud
# If Prophet or LightGBM installation fails on Streamlit Cloud, try removing them and using pre-built artifacts.
